[project]
name = "codex-usage-widget"
version = "0.1.0"
description = "Always-on-top Codex usage widget powered by Codex app-server."
readme = "AGENTS.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
    "pystray>=0.19.5",
]

[tool.uv]
package = false
